定义控制流关键字就是列表,"定义","如果","那么","否则","对于","跳出","继续","返回","导入","导出","赋值"

定义强制拆分关键字就是列表,"定义","如果","那么","否则","对于","跳出","继续","返回","导入","导出","赋值","就是"

定义单字关键字就是列表,"从","到","为"

定义四字关键字就是列表,"大于等于","小于等于","对于每个"

定义三字关键字就是列表,"就是函","就是宏","否则若","当满足","定义宏","不等于"

定义双字关键字就是列表,"定义","如果","那么","否则","对于","跳出","继续","返回","导入","导出","赋值","就是","列表","索引","长度","打印","等于","不等","大于","小于","真值","假值","空值","然后","生成","捕获","任意","列表修改","消息拼接","字符串索引","子字符串","字符串长度","字符串转列表","列表转字符串","字符串前缀","字符串包含","数字转字符串","字符串转数字","是否相等"

定义双字运算符就是列表,"加","减","乘","除","模","幂"

定义标识符读边界关键字就是列表,"就是","从","到","为","然后","经过","大于","小于","等于","不等","那么"

定义单字运算符就是列表,"加","减","乘","除","模","幂","非","与","或"

定义创建Token就是函类型,值,行,列：
  列表,"token",类型,值,行,列

定义检查边界关键字就是函词：
  定义词长就是(字符串长度,词)
  如果词长等于1那么：
    对于每个当前词从单字关键字：
      如果(是否相等,当前词,词)那么：返回真值
    返回假值
  否则若词长等于2那么：
    定义候选就是列表,"定义","如果","那么","否则","对于","跳出","继续","返回","导入","导出","赋值","就是","等于","不等","大于","小于","然后","加","减","乘","除","模","幂","非","与","或"
    对于每个当前词从候选：
      如果(是否相等,当前词,词)那么：返回真值
    返回假值
  否则若词长等于3那么：
    对于每个当前词从三字关键字：
      如果(是否相等,当前词,词)那么：返回真值
    返回假值
  否则若词长等于4那么：
    对于每个当前词从四字关键字：
      如果(是否相等,当前词,词)那么：返回真值
    返回假值
  否则：返回假值

定义是边界关键字就是函源码,长度,位置：
  如果位置大于等于长度那么：返回假值
  定义当前字符就是字符串索引(源码,位置)
  如果(列表包含,单字关键字,列表转字符串(列表(当前字符)))那么：返回真值
  如果(列表包含,单字运算符,列表转字符串(列表(当前字符)))那么：返回真值
  如果(加,位置,1)小于长度那么：
    如果(列表包含,标识符读边界关键字,子字符串(源码,位置,(加,位置,2)))那么：返回真值
  如果(加,位置,2)小于长度那么：
    如果(列表包含,三字关键字,子字符串(源码,位置,(加,位置,3)))那么：返回真值
  如果(加,位置,3)小于长度那么：
    如果(列表包含,四字关键字,子字符串(源码,位置,(加,位置,4)))那么：返回真值
  返回假值

定义读字符串就是函源码,长度,位置,行,列,引号：
  定义收集就是空值
  定义循环就是函位置,行,列,收集：
    如果位置大于等于长度那么：
      报错,"未闭合的字符串"
    否则：
      定义当前字符就是字符串索引(源码,位置)
      如果是否相等(当前字符,引号)那么：
        列表(创建Token("STRING",列表转字符串(反转(收集)),行,列),(加,位置,1),行,(加,列,1))
      否则若是否相等(当前字符,字符(10))那么：
        循环((加,位置,1),(加,行,1),1,前置(当前字符,收集))
      否则若是否相等(当前字符,字符(92))那么：
        定义转义位置就是(加,位置,1)
        如果转义位置小于长度那么：
          定义转义字符就是字符串索引(源码,转义位置)
          如果是否相等(转义字符,字符(110))那么：
            循环((加,位置,2),行,(加,列,2),前置(字符(10),收集))
          否则若是否相等(转义字符,字符(116))那么：
            循环((加,位置,2),行,(加,列,2),前置(字符(9),收集))
          否则若是否相等(转义字符,字符(92))那么：
            循环((加,位置,2),行,(加,列,2),前置(字符(92),收集))
          否则若是否相等(转义字符,字符(34))那么：
            循环((加,位置,2),行,(加,列,2),前置(字符(34),收集))
          否则：
            循环((加,位置,2),行,(加,列,2),前置(转义字符,收集))
        否则：
          报错,"不完整的转义序列"
      否则：
        循环((加,位置,1),行,(加,列,1),前置(当前字符,收集))
  循环((加,位置,1),行,(加,列,1),收集)

定义读数字就是函源码,长度,位置,行,列：
  定义起始列就是列
  定义收集就是列表(字符串索引(源码,位置))
  定义循环就是函位置,列,收集：
    如果位置大于等于长度那么：
      定义数字字符串就是列表转字符串(反转(收集))
      列表(创建Token("NUMBER",字符串转数字(数字字符串),行,起始列),位置,列)
    否则：
      定义当前字符就是字符串索引(源码,位置)
      如果是数字(当前字符)那么：
        循环((加,位置,1),(加,列,1),前置(当前字符,收集))
      否则若是否相等(当前字符,字符(46))那么：
        循环((加,位置,1),(加,列,1),前置(当前字符,收集))
      否则：
        定义数字字符串就是列表转字符串(反转(收集))
        列表(创建Token("NUMBER",字符串转数字(数字字符串),行,起始列),位置,列)
  循环((加,位置,1),(加,列,1),收集)

定义读标识符收集就是函源码,长度,位置,行,列,收集：
  如果位置大于等于长度那么：
    创建Token("IDENTIFIER",列表转字符串(反转(收集)),行,列)
  否则：
    定义当前字符就是字符串索引(源码,位置)
    如果是中文(当前字符)那么：
      如果是边界关键字(源码,长度,位置)那么：
        创建Token("IDENTIFIER",列表转字符串(反转(收集)),行,列)
      否则：
        读标识符收集(源码,长度,(加,位置,1),行,列,前置(当前字符,收集))
    否则若(是字母(当前字符)或是数字(当前字符)或是否相等(当前字符,字符(95)))那么：
      读标识符收集(源码,长度,(加,位置,1),行,列,前置(当前字符,收集))
    否则：
      创建Token("IDENTIFIER",列表转字符串(反转(收集)),行,列)

定义读标识符自就是函源码,长度,位置,行,列：
  定义当前字符就是字符串索引(源码,位置)
  读标识符收集(源码,长度,(加,位置,1),行,列,列表(当前字符))

定义计算缩进就是函源码,长度,位置：
  如果位置大于等于长度那么：
    列表(0,位置)
  否则：
    定义当前字符就是字符串索引(源码,位置)
    如果是否相等(当前字符,字符(32))那么：
      定义结果就是计算缩进(源码,长度,(加,位置,1))
      列表((加,索引(结果,0),1),索引(结果,1))
    否则若是否相等(当前字符,字符(9))那么：
      定义结果就是计算缩进(源码,长度,(加,位置,4))
      列表((加,索引(结果,0),4),索引(结果,1))
    否则：
      列表(0,位置)

定义处理换行就是函源码,长度,位置,行,列,缩进栈,结果：
  定义新行就是(加,行,1)
  定义缩进结果就是计算缩进(源码,长度,位置)
  定义新缩进就是索引(缩进结果,0)
  定义跳过位置就是索引(缩进结果,1)
  定义当前缩进就是索引(缩进栈,0)
  如果新缩进大于当前缩进那么：
    定义缩进令牌就是创建Token("INDENT",空值,新行,1)
    定义换行令牌就是创建Token("NEWLINE",空值,新行,列)
    定义新结果就是前置(新缩进,缩进栈)
    定义令牌列表就是前置(缩进令牌,前置(换行令牌,结果))
    (分词循环,源码,长度,跳过位置,新行,1,新结果,令牌列表)
  否则若新缩进小于当前缩进那么：
    定义新栈就是去掉首个(缩进栈)
    如果新缩进小于索引(新栈,0)那么：
      定义退栈令牌就是前置(创建Token("DEDENT",空值,新行,列),结果)
      处理换行(源码,长度,跳过位置,新行,1,新栈,退栈令牌)
    否则若新缩进等于索引(新栈,0)那么：
      定义退栈令牌就是创建Token("DEDENT",空值,新行,列)
      定义换行令牌就是创建Token("NEWLINE",空值,新行,列)
      定义令牌列表就是前置(退栈令牌,前置(换行令牌,结果))
      (分词循环,源码,长度,跳过位置,新行,1,新栈,令牌列表)
    否则：
      定义换行令牌就是前置(创建Token("NEWLINE",空值,新行,列),结果)
      (分词循环,源码,长度,跳过位置,新行,1,缩进栈,换行令牌)
  否则：
    定义换行令牌就是前置(创建Token("NEWLINE",空值,新行,列),结果)
    (分词循环,源码,长度,跳过位置,新行,1,缩进栈,换行令牌)

定义生成退栈就是函缩进栈,行,列,结果：
  定义栈长就是(长度,缩进栈)
  如果栈长小于等于1那么：
    结果
  否则：
    定义退栈令牌就是前置(创建Token("DEDENT",空值,行,列),结果)
    生成退栈(去掉首个(缩进栈),行,列,退栈令牌)

定义是强制拆分就是函词：
  如果是否相等(词,"定义")那么：
    返回真值
  否则若是否相等(词,"如果")那么：
    返回真值
  否则若是否相等(词,"那么")那么：
    返回真值
  否则若是否相等(词,"否则")那么：
    返回真值
  否则若是否相等(词,"对于")那么：
    返回真值
  否则若是否相等(词,"跳出")那么：
    返回真值
  否则若是否相等(词,"继续")那么：
    返回真值
  否则若是否相等(词,"返回")那么：
    返回真值
  否则若是否相等(词,"导入")那么：
    返回真值
  否则若是否相等(词,"导出")那么：
    返回真值
  否则若是否相等(词,"赋值")那么：
    返回真值
  否则若是否相等(词,"就是")那么：
    返回真值
  否则：
    返回假值

定义是双字关键字就是函词：
  对于每个当前词从双字关键字：
    如果是否相等(当前词,词)那么：
      返回真值
  返回假值

定义是单字关键字就是函词：
  对于每个当前词从单字关键字：
    如果是否相等(当前词,词)那么：
      返回真值
  返回假值

定义是单字运算符就是函词：
  对于每个当前词从单字运算符：
    如果是否相等(当前词,词)那么：
      返回真值
  返回假值

定义是双字运算符就是函词：
  对于每个当前词从双字运算符：
    如果是否相等(当前词,词)那么：
      返回真值
  返回假值

定义尝试读关键字标识符就是函源码,长度,位置,行,列,缩进栈,结果：
  定义当前位置就是位置
  如果(加,位置,3)小于长度那么：
    定义四字词就是子字符串(源码,位置,(加,位置,4))
    对于每个关键字从四字关键字：
      如果是否相等(关键字,四字词)那么：
        定义新位置就是(加,位置,4)
        定义新列就是(加,列,4)
        定义令牌就是创建Token("KEYWORD",四字词,行,列)
        返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
  如果(加,位置,2)小于长度那么：
    定义三字词就是子字符串(源码,位置,(加,位置,3))
    对于每个关键字从三字关键字：
      如果是否相等(关键字,三字词)那么：
        定义新位置就是(加,位置,3)
        定义新列就是(加,列,3)
        定义令牌就是创建Token("KEYWORD",三字词,行,列)
        返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
  如果(加,位置,1)小于长度那么：
    定义两字词就是子字符串(源码,位置,(加,位置,2))
    如果(是双字关键字,两字词)那么：
      如果(是强制拆分,两字词)那么：
        定义新位置就是(加,位置,2)
        定义新列就是(加,列,2)
        定义令牌就是创建Token("KEYWORD",两字词,行,列)
        返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
      否则：
        如果(加,位置,2)小于长度那么：
          定义三字字符就是字符串索引(源码,(加,位置,2))
          如果是中文(三字字符)那么：
            如果(加,位置,3)小于长度那么：
              定义后续两字就是子字符串(源码,(加,位置,2),(加,位置,4))
              如果(是双字关键字,后续两字)那么：
                定义新位置就是(加,位置,2)
                定义新列就是(加,列,2)
                定义令牌就是创建Token("KEYWORD",两字词,行,列)
                返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
              否则：
                定义token就是读标识符自(源码,长度,位置,行,列)
                定义新位置就是(加,位置,字符串长度(索引(token,2)))
                返回(分词循环,源码,长度,新位置,行,列,缩进栈,前置(token,结果))
            否则：
              定义新位置就是(加,位置,2)
              定义新列就是(加,列,2)
              定义令牌就是创建Token("KEYWORD",两字词,行,列)
              返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
          否则：
            定义新位置就是(加,位置,2)
            定义新列就是(加,列,2)
            定义令牌就是创建Token("KEYWORD",两字词,行,列)
            返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
        否则：
          定义新位置就是(加,位置,2)
          定义新列就是(加,列,2)
          定义令牌就是创建Token("KEYWORD",两字词,行,列)
          返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
    否则若(是双字运算符,两字词)那么：
      定义新位置就是(加,位置,2)
      定义新列就是(加,列,2)
      定义令牌就是创建Token("OPERATOR",两字词,行,列)
      返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
  定义单字词就是子字符串(源码,位置,(加,位置,1))
  如果(是单字关键字,单字词)那么：
    定义新位置就是(加,位置,1)
    定义新列就是(加,列,1)
    定义令牌就是创建Token("KEYWORD",单字词,行,列)
    返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
  如果(是单字运算符,单字词)那么：
    如果(加,位置,1)小于长度那么：
      定义下一个字符就是字符串索引(源码,(加,位置,1))
      如果是中文(下一个字符)那么：
        如果(加,位置,2)小于长度那么：
          定义下个两字就是子字符串(源码,(加,位置,1),(加,位置,3))
          如果不是(是双字关键字,下个两字)那么：
            定义token就是读标识符自(源码,长度,位置,行,列)
            定义新位置就是(加,位置,字符串长度(索引(token,2)))
            返回(分词循环,源码,长度,新位置,行,列,缩进栈,前置(token,结果))
          否则：
            定义新位置就是(加,位置,1)
            定义新列就是(加,列,1)
            定义令牌就是创建Token("OPERATOR",单字词,行,列)
            返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
        否则：
          定义token就是读标识符自(源码,长度,位置,行,列)
          定义新位置就是(加,位置,字符串长度(索引(token,2)))
          返回(分词循环,源码,长度,新位置,行,列,缩进栈,前置(token,结果))
      否则：
        定义新位置就是(加,位置,1)
        定义新列就是(加,列,1)
        定义令牌就是创建Token("OPERATOR",单字词,行,列)
        返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
    否则：
      定义新位置就是(加,位置,1)
      定义新列就是(加,列,1)
      定义令牌就是创建Token("OPERATOR",单字词,行,列)
      返回(分词循环,源码,长度,新位置,行,新列,缩进栈,前置(令牌,结果))
  定义token就是读标识符自(源码,长度,位置,行,列)
  定义新位置就是(加,位置,字符串长度(索引(token,2)))
  返回(分词循环,源码,长度,新位置,行,列,缩进栈,前置(token,结果))

定义分词循环就是函源码,长度,位置,行,列,缩进栈,结果：
  如果位置大于等于长度那么：
    反转(生成退栈(缩进栈,行,列,结果))
  否则：
    定义当前字符就是字符串索引(源码,位置)
    如果是换行(当前字符)那么：
      如果(加,位置,1)小于长度那么：
        定义下一个就是字符串索引(源码,(加,位置,1))
        如果是否相等(当前字符,字符(13))与是否相等(下一个,字符(10))那么：
          处理换行(源码,长度,(加,位置,2),行,列,缩进栈,结果)
        否则：
          处理换行(源码,长度,(加,位置,1),行,列,缩进栈,结果)
      否则：
        处理换行(源码,长度,(加,位置,1),行,列,缩进栈,结果)
    否则若是空白(当前字符)那么：
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,结果)
    否则若是否相等(当前字符,字符(13))那么：
      分词循环(源码,长度,(加,位置,1),行,列,缩进栈,结果)
    否则若是否相等(当前字符,字符(35))或是否相等(当前字符,字符(59))那么：
      定义跳过注释就是函位置：
        如果位置大于等于长度那么：位置
        否则若是换行(字符串索引(源码,位置))那么：位置
        否则：跳过注释((加,位置,1))
      定义新位置就是跳过注释((加,位置,1))
      分词循环(源码,长度,新位置,行,列,缩进栈,结果)
    否则若是否相等(当前字符,字符(34))或是否相等(当前字符,字符(39))那么：
      定义结果列表就是读字符串(源码,长度,位置,行,列,当前字符)
      定义token就是索引(结果列表,0)
      定义新位置就是索引(结果列表,1)
      定义新行就是索引(结果列表,2)
      定义新列就是索引(结果列表,3)
      分词循环(源码,长度,新位置,新行,新列,缩进栈,前置(token,结果))
    否则若是数字(当前字符)那么：
      定义结果列表就是读数字(源码,长度,位置,行,列)
      定义token就是索引(结果列表,0)
      定义新位置就是索引(结果列表,1)
      定义新列就是索引(结果列表,2)
      分词循环(源码,长度,新位置,行,新列,缩进栈,前置(token,结果))
    否则若是中文(当前字符)那么：
      尝试读关键字标识符(源码,长度,位置,行,列,缩进栈,结果)
    否则若是否相等(当前字符,字符(58))或是否相等(当前字符,字符(65306))那么：
      定义令牌就是创建Token("COLON",字符(58),行,列)
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,前置(令牌,结果))
    否则若是否相等(当前字符,字符(44))或是否相等(当前字符,字符(65292))那么：
      定义令牌就是创建Token("COMMA",当前字符,行,列)
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,前置(令牌,结果))
    否则若是否相等(当前字符,字符(124))那么：
      定义令牌就是创建Token("PIPE",当前字符,行,列)
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,前置(令牌,结果))
    否则若是否相等(当前字符,字符(12290))那么：
      定义令牌就是创建Token("DOT",当前字符,行,列)
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,前置(令牌,结果))
    否则若是否相等(当前字符,字符(40))或是否相等(当前字符,字符(65288))那么：
      定义令牌就是创建Token("LPAREN",空值,行,列)
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,前置(令牌,结果))
    否则若是否相等(当前字符,字符(41))或是否相等(当前字符,字符(65289))那么：
      定义令牌就是创建Token("RPAREN",空值,行,列)
      分词循环(源码,长度,(加,位置,1),行,(加,列,1),缩进栈,前置(令牌,结果))
    否则若(是字母(当前字符)或(是否相等(当前字符,字符(95))))那么：
      定义token就是读标识符自(源码,长度,位置,行,列)
      定义新位置就是(加,位置,字符串长度(索引(token,2)))
      分词循环(源码,长度,新位置,行,列,缩进栈,前置(token,结果))
    否则：
      报错,"无法识别的字符"

定义分词就是函源码：
  定义源码长度就是字符串长度(源码)
  分词循环(源码,源码长度,0,1,1,列表(0),空值)