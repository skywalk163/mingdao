;; ============================================================
;; utils.mingdao — 通用辅助函数模块
;; 提供：交换、比较、打印辅助
;; ============================================================

;; ---- 交换函数：交换列表中两个位置的元素 ----
定义 交换 就是函 数组, i, j：
    定义 值i 就是 索引, 数组, i
    定义 值j 就是 索引, 数组, j
    定义 临时1 就是 列表修改, 数组, i, 值j
    返回 列表修改, 临时1, j, 值i

;; ---- 比较函数：比较两个元素 ----
;; 返回：1 表示 a>b, 0 表示 a==b, 2 表示 a<b
定义 比较 就是函 a, b：
    如果 a 小于 b 那么：
        返回 2
    否则若 a 大于 b 那么：
        返回 1
    否则：
        返回 0

;; ---- 打印数组辅助函数 ----
定义 打印数组 就是函 前缀, 数组：
    打印, (拼接, 前缀, 数组)

;; ---- 导出公开符号 ----
导出 交换 比较 打印数组
