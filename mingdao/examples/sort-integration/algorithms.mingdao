;; ============================================================
;; algorithms.mingdao — 排序算法集合模块
;; 提供：冒泡排序、插入排序、选择排序
;; 依赖：utils.mingdao (交换、比较、打印数组)
;; ============================================================

导入 "utils.mingdao"

;; ---- 算法1：冒泡排序 ----
定义 冒泡排序 就是函 输入数组：
    定义 数组长度 就是 长度, 输入数组
    定义 数组 就是 输入数组
    对于 i 从 0 到 数组长度 减 1：
        对于 j 从 0 到 数组长度 减 i 减 1：
            定义 当前 就是 索引, 数组, j
            定义 下一个 就是 索引, 数组, (j 加 1)
            如果 当前 大于 下一个 那么：
                赋值 数组 为 交换, 数组, j, (j 加 1)
    返回 数组

;; ---- 算法2：插入排序 ----
定义 插入排序 就是函 输入数组：
    定义 数组长度 就是 长度, 输入数组
    定义 数组 就是 输入数组
    对于 i 从 1 到 数组长度 减 1：
        定义 当前值 就是 索引, 数组, i
        定义 位置 就是 i
        对于 j 从 0 到 i：
            如果 位置 大于 0 那么：
                定义 前值 就是 索引, 数组, (位置 减 1)
                如果 前值 大于 当前值 那么：
                    赋值 数组 为 交换, 数组, 位置, (位置 减 1)
                    赋值 位置 为 位置 减 1
    返回 数组

;; ---- 算法3：选择排序 ----
定义 选择排序 就是函 输入数组：
    定义 数组长度 就是 长度, 输入数组
    定义 数组 就是 输入数组
    对于 i 从 0 到 数组长度 减 1：
        定义 最小索引 就是 i
        定义 最小值 就是 索引, 数组, i
        对于 j 从 (i 加 1) 到 数组长度 减 1：
            定义 当前值 就是 索引, 数组, j
            如果 当前值 小于 最小值 那么：
                赋值 最小值 为 当前值
                赋值 最小索引 为 j
        如果 最小索引 大于 i 那么：
            赋值 数组 为 交换, 数组, i, 最小索引
    返回 数组

;; ---- 导出所有排序算法 ----
导出 冒泡排序 插入排序 选择排序
