;; ============================================================
;; 模块：collision.mingdao — 碰撞检测
;; 功能：子弹打敌机、敌弹打玩家
;; 依赖：constants.mingdao, state.mingdao, helper.mingdao
;; 调试：可用 检查 查看实体列表变化
;; ============================================================

;; ---- 子弹 vs 敌机 ----
定义 检测子弹打敌机 就是函 _：
    定义 新子弹列表 就是 列表
    对于 i 从 0 到 子弹列表, 长度：
        定义 子弹 就是 索引, 子弹列表, i
        定义 bx 就是 子弹, 0, 索引
        定义 by 就是 子弹, 1, 索引
        定义 bd 就是 子弹, 3, 索引
        定义 击中 就是 假值
        对于 j 从 0 到 敌机列表, 长度：
            定义 敌机 就是 索引, 敌机列表, j
            定义 hp 就是 敌机, 4, 索引
            如果 hp 大于 0 那么：
                定义 ex 就是 敌机, 0, 索引
                定义 ey 就是 敌机, 1, 索引
                定义 bd 就是 子弹, 3, 索引
                定义 类型 就是 敌机, 5, 索引
                定义 ew 就是 敌机, 6, 索引
                定义 eh 就是 敌机, 7, 索引
                如果 bx, by, bx 加 6, by 加 14, ex, ey, ex 加 ew, ey 加 eh, 矩形碰撞 那么：
                    赋值 击中 为 真值
                    定义 新hp 就是 hp 减 bd
                    如果 新hp 小于等于 0 那么：
                        定义 空敌 就是 列表 0, 0, 0, 0, 0, 0, 0, 0
                        赋值 敌机列表 为 列表修改, 敌机列表, j, 空敌
                        定义 加分 就是 10
                        如果 类型 等于 1 那么：
                            赋值 加分 为 30
                        如果 类型 等于 2 那么：
                            赋值 加分 为 200
                        赋值 分数 为 分数 加 加分
                        定义 新炸 就是 列表 ex, ey, 0, 20
                        赋值 爆炸列表 为 追加, 爆炸列表, 新炸
                        如果 类型 等于 2 那么：
                            赋值 升级道具x 为 ex 加 30
                            赋值 升级道具y 为 ey
                            赋值 已有升级 为 真值
                    否则：
                        定义 vx 就是 敌机, 2, 索引
                        定义 vy 就是 敌机, 3, 索引
                        定义 新敌 就是 列表 ex, ey, vx, vy, 新hp, 类型, ew, eh
                        赋值 敌机列表 为 列表修改, 敌机列表, j, 新敌
                    跳出
        如果 击中 等于 假值 那么：
            赋值 新子弹列表 为 追加, 新子弹列表, 子弹
    赋值 子弹列表 为 新子弹列表

;; ---- 敌弹 vs 玩家 ----
定义 检测敌弹打玩家 就是函 _：
    如果 无敌计时 大于 0 那么：
        返回
    定义 新敌弹 就是 列表
    对于 i 从 0 到 敌弹列表, 长度：
        定义 弹 就是 索引, 敌弹列表, i
        定义 bx 就是 弹, 0, 索引
        定义 by 就是 弹, 1, 索引
        定义 击中 就是 假值
        如果 bx, by, bx 加 8, by 加 8, 玩家x, 玩家y, 玩家x 加 玩家宽, 玩家y 加 玩家高, 矩形碰撞 那么：
            赋值 击中 为 真值
            赋值 玩家生命 为 玩家生命 减 1
            赋值 无敌计时 为 无敌时间
            定义 新炸 就是 列表 玩家x 加 20, 玩家y 加 22, 0, 15
            赋值 爆炸列表 为 追加, 爆炸列表, 新炸
            如果 玩家生命 小于等于 0 那么：
                赋值 游戏结束 为 真值
        如果 击中 等于 假值 那么：
            赋值 新敌弹 为 追加, 新敌弹, 弹
    赋值 敌弹列表 为 新敌弹