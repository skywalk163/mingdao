;; ============================================================
;; 模块：logic.mingdao — 游戏逻辑
;; 功能：玩家移动、射击、敌机生成与移动、爆炸更新、道具
;; 依赖：所有其他模块
;; 调试：可在主循环每次迭代检查关键变量
;; ============================================================

;; ---- 玩家移动 ----
定义 移动玩家 就是函 _：
    定义 dt 就是 帧时间
    如果 按键按下, "left" 那么：
        赋值 玩家x 为 玩家x 减 玩家速度 乘 dt
    如果 按键按下, "right" 那么：
        赋值 玩家x 为 玩家x 加 玩家速度 乘 dt
    如果 按键按下, "up" 那么：
        赋值 玩家y 为 玩家y 减 玩家速度 乘 dt
    如果 按键按下, "down" 那么：
        赋值 玩家y 为 玩家y 加 玩家速度 乘 dt
    如果 玩家x 小于 0 那么：
        赋值 玩家x 为 0
    如果 玩家x 大于 屏幕宽 减 玩家宽 那么：
        赋值 玩家x 为 屏幕宽 减 玩家宽
    如果 玩家y 小于 0 那么：
        赋值 玩家y 为 0
    如果 玩家y 大于 屏幕高 减 玩家高 那么：
        赋值 玩家y 为 屏幕高 减 玩家高

;; ---- 发射子弹 ----
定义 发射子弹 就是函 _：
    如果 射击计时 大于 0 那么：
        返回
    赋值 射击计时 为 射击间隔
    定义 cx 就是 玩家x 加 玩家宽 除 2
    定义 cy 就是 玩家y
    如果 武器等级 等于 1 那么：
        定义 弹 就是 列表 cx 减 3, cy 减 10, 0, 子弹速度, 1
        赋值 子弹列表 为 追加, 子弹列表, 弹
    否则若 武器等级 等于 2 那么：
        定义 弹1 就是 列表 cx 减 8, cy 减 10, 0, 子弹速度, 1
        定义 弹2 就是 列表 cx 加 2, cy 减 10, 0, 子弹速度, 1
        赋值 子弹列表 为 追加, 子弹列表, 弹1
        赋值 子弹列表 为 追加, 子弹列表, 弹2
    否则：
        定义 弹1 就是 列表 cx 减 10, cy 减 8, 0, 子弹速度, 2
        定义 弹2 就是 列表 cx 减 3, cy 减 12, 0, 子弹速度, 2
        定义 弹3 就是 列表 cx 加 4, cy 减 8, 0, 子弹速度, 2
        赋值 子弹列表 为 追加, 子弹列表, 弹1
        赋值 子弹列表 为 追加, 子弹列表, 弹2
        赋值 子弹列表 为 追加, 子弹列表, 弹3

;; ---- 子弹更新 ----
定义 更新子弹 就是函 _：
    定义 新列表 就是 列表
    定义 dt 就是 帧时间
    对于 i 从 0 到 子弹列表, 长度：
        定义 子弹 就是 索引, 子弹列表, i
        定义 bx 就是 子弹, 0, 索引
        定义 by 就是 子弹, 1, 索引
        定义 bv 就是 子弹, 3, 索引
        定义 ny 就是 by 减 bv 乘 dt
        如果 ny 大于 减 20 那么：
            定义 原伤害 就是 子弹, 4, 索引
            定义 新子弹 就是 列表 bx, ny, 0, bv, 原伤害
            赋值 新列表 为 追加, 新列表, 新子弹
    赋值 子弹列表 为 新列表

;; ---- 敌弹更新 ----
定义 更新敌弹 就是函 _：
    定义 新列表 就是 列表
    定义 dt 就是 帧时间
    对于 i 从 0 到 敌弹列表, 长度：
        定义 弹 就是 索引, 敌弹列表, i
        定义 bx 就是 弹, 0, 索引
        定义 by 就是 弹, 1, 索引
        定义 ny 就是 by 加 敌弹速度 乘 dt
        如果 ny 小于 屏幕高 加 20 那么：
            定义 新弹 就是 列表 bx, ny
            赋值 新列表 为 追加, 新列表, 新弹
    赋值 敌弹列表 为 新列表

;; ---- 敌机生成 ----
定义 生成敌机 就是函 _：
    定义 随机值 就是 1, 100, 随机范围
    如果 随机值 大于 95 那么：
        定义 ex 就是 10, 屏幕宽 减 60, 随机范围
        如果 Boss出现 等于 假值 那么：
            如果 分数 大于 100 那么：
                赋值 Boss出现 为 真值
                定义 新敌 就是 列表 ex, 减 50, 1, 80, 25, 2, 80, 70
                赋值 敌机列表 为 追加, 敌机列表, 新敌
            否则：
                定义 新敌 就是 列表 ex, 减 30, 0, 50, 1, 0, 24, 24
                赋值 敌机列表 为 追加, 敌机列表, 新敌
        否则：
            定义 类型 就是 0
            如果 分数 大于 50 那么：
                如果 随机值 大于 98 那么：
                    赋值 类型 为 1
            定义 ew 就是 24
            定义 eh 就是 24
            如果 类型 等于 1 那么：
                赋值 ew 为 36
                赋值 eh 为 36
            定义 新敌 就是 列表 ex, 减 30, 0, 50, 1, 类型, ew, eh
            赋值 敌机列表 为 追加, 敌机列表, 新敌

;; ---- 敌机移动 ----
定义 移动敌机 就是函 _：
    定义 新列表 就是 列表
    定义 dt 就是 帧时间
    对于 i 从 0 到 敌机列表, 长度：
        定义 敌机 就是 索引, 敌机列表, i
        定义 ex 就是 敌机, 0, 索引
        定义 ey 就是 敌机, 1, 索引
        定义 vx 就是 敌机, 2, 索引
        定义 vy 就是 敌机, 3, 索引
        定义 hp 就是 敌机, 4, 索引
        定义 类型 就是 敌机, 5, 索引
        定义 ew 就是 敌机, 6, 索引
        定义 eh 就是 敌机, 7, 索引
        定义 ny 就是 ey 加 vy 乘 dt
        定义 nx 就是 ex 加 vx 乘 dt
        如果 hp 大于 0 那么：
            如果 ny 小于 屏幕高 加 50 那么：
                定义 新敌 就是 列表 nx, ny, vx, vy, hp, 类型, ew, eh
                赋值 新列表 为 追加, 新列表, 新敌
            如果 类型 等于 1 那么：
                如果 (1, 100, 随机范围) 大于 98 那么：
                    定义 新弹 就是 列表 ex 加 14, ey 加 36
                    赋值 敌弹列表 为 追加, 敌弹列表, 新弹
            如果 类型 等于 2 那么：
                如果 (1, 100, 随机范围) 大于 95 那么：
                    定义 弹1 就是 列表 ex 加 20, ey 加 60
                    定义 弹2 就是 列表 ex 加 60, ey 加 60
                    赋值 敌弹列表 为 追加, 敌弹列表, 弹1
                    赋值 敌弹列表 为 追加, 敌弹列表, 弹2
    赋值 敌机列表 为 新列表

;; ---- 爆炸更新 ----
定义 更新爆炸 就是函 _：
    定义 新列表 就是 列表
    对于 i 从 0 到 爆炸列表, 长度：
        定义 爆 就是 索引, 爆炸列表, i
        定义 bt 就是 爆, 2, 索引
        定义 bm 就是 爆, 3, 索引
        定义 nbt 就是 bt 加 1
        如果 nbt 小于 bm 那么：
            定义 x 就是 爆, 0, 索引
            定义 y 就是 爆, 1, 索引
            定义 新爆 就是 列表 x, y, nbt, bm
            赋值 新列表 为 追加, 新列表, 新爆
    赋值 爆炸列表 为 新列表

;; ---- 升级道具 ----
定义 更新升级道具 就是函 _：
    如果 已有升级 等于 真值 那么：
        定义 ny 就是 升级道具y 加 100 乘 帧时间
        赋值 升级道具y 为 ny
        画实心圆形, 升级道具x, 升级道具y, 12, 255, 255, 0
        画实心圆形, 升级道具x, 升级道具y, 6, 255, 0, 255
        定义 px 就是 玩家x
        定义 py 就是 玩家y
        如果 升级道具x, 升级道具y, 升级道具x 加 24, 升级道具y 加 24, px, py, px 加 玩家宽, py 加 玩家高, 矩形碰撞 那么：
            如果 武器等级 小于 3 那么：
                赋值 武器等级 为 武器等级 加 1
            赋值 已有升级 为 假值
        如果 ny 大于 屏幕高 那么：
            赋值 已有升级 为 假值

;; ---- 主更新入口 ----
定义 更新 就是函 _：
    定义 dt 就是 帧时间
    赋值 射击计时 为 射击计时 减 dt
    如果 射击计时 小于 0 那么：
        赋值 射击计时 为 0
    赋值 无敌计时 为 无敌计时 减 dt
    如果 无敌计时 小于 0 那么：
        赋值 无敌计时 为 0
    如果 游戏结束 那么：
        如果 按键按下, "r" 那么：
            空值, 重置游戏
        返回
    空值, 移动玩家
    如果 按键按下, "z" 那么：
        空值, 发射子弹
    空值, 更新子弹
    空值, 更新敌弹
    空值, 生成敌机
    空值, 移动敌机
    空值, 检测子弹打敌机
    空值, 检测敌弹打玩家
    空值, 更新爆炸
    空值, 更新升级道具