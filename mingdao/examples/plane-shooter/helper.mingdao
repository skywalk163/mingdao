;; ============================================================
;; 模块：helper.mingdao — 辅助函数
;; 功能：随机数、碰撞检测等通用函数
;; 调试：可用 测试组 对每个函数做单元测试
;; ============================================================

;; ---- 随机范围 ----
定义 随机范围 就是函 最小, 最大：
    返回 最小, 最大, 随机整数

;; ---- AABB 矩形碰撞检测 ----
;; 参数：矩形A左上右下 (ax1,ay1)-(ax2,ay2)
;;       矩形B左上右下 (bx1,by1)-(bx2,by2)
;; 返回：真值（碰撞）或 假值（未碰撞）
定义 矩形碰撞 就是函 ax1, ay1, ax2, ay2, bx1, by1, bx2, by2：
    断言, ax1 小于 ax2, "矩形A水平范围无效"
    断言, ay1 小于 ay2, "矩形A垂直范围无效"
    断言, bx1 小于 bx2, "矩形B水平范围无效"
    断言, by1 小于 by2, "矩形B垂直范围无效"
    如果 ax2 大于 bx1 那么：
        如果 ax1 小于 bx2 那么：
            如果 ay2 大于 by1 那么：
                如果 ay1 小于 by2 那么：
                    返回 真值
    返回 假值