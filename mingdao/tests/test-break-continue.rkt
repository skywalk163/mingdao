#lang mingdao

打印, "========== 当满足 + 跳出 测试 =========="

定义 n 就是 1
当满足 真值 那么：
    打印, n
    如果 n 等于 5 那么：
        跳出
    赋值 n 为 n 加 1

打印, "========== 当满足 + 继续 测试 =========="

定义 m 就是 0
当满足 m 小于 10 那么：
    赋值 m 为 m 加 1
    如果 m 大于 5 那么：
        继续
    打印, m

打印, "========== 对于 + 跳出 测试 =========="

对于 i 从 1 到 10：
    打印, i
    如果 i 等于 3 那么：
        跳出

打印, "========== 对于 + 继续 测试 =========="

对于 i 从 1 到 6：
    如果 i 等于 3 那么：
        继续
    如果 i 等于 5 那么：
        继续
    打印, i

打印, "========== 对于每个 + 跳出 测试 =========="

定义 lst 就是列表 1, 2, 3, 4, 5, 6, 7
对于每个 x 从 lst：
    如果 x 大于 4 那么：
        跳出
    打印, x

打印, "========== 对于每个 + 继续 测试 =========="

定义 lst2 就是列表 1, 2, 3, 4, 5, 6
对于每个 x 从 lst2：
    如果 x 等于 2 那么：
        继续
    如果 x 等于 4 那么：
        继续
    打印, x

打印, "========== 所有循环控制测试完成 =========="